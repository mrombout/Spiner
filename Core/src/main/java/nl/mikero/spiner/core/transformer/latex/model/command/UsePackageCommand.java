package nl.mikero.spiner.core.transformer.latex.model.command;

public class UsePackageCommand extends AbstractCommand {
    private static final String COMMAND_NAME = "usepackage";

    public UsePackageCommand() {
        super(COMMAND_NAME);
    }
}
