/**
 * nl.mikero.spiner.core.twine.markdown contains Markdown parsers and renders tuned to match the flavours used by
 * different Twine formats.
 *
 * In Twine a "Story Format" is responsible for parsing a passage's content. At the time or writing, all official story
 * formats use Markdown, but not all may use the same version.
 */
package nl.mikero.spiner.core.twine.markdown;